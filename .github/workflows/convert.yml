name: Convert to MLC

on:
  workflow_dispatch:

jobs:
  convert:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install MLC-LLM
        run: |
          pip install --upgrade pip
          pip install mlc-llm -f https://mlc.ai/wheels --pre
          pip install huggingface_hub hf_transfer

      - name: Download Serow model
        env:
          HF_HUB_ENABLE_HF_TRANSFER: 1
        run: |
          hf download WebAIPocket/Serow-Qwen2.5-0.5B-Instruct --local-dir ./model --local-dir-use-symlinks False

      - name: Create MLC repo if not exists
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          hf repo create WebAIPocket/Serow-Qwen2.5-0.5B-Instruct-q4f16_1-MLC --repo-type model --exist-ok --token $HF_TOKEN

      - name: Convert to MLC (q4f16_1)
        run: |
          mlc_llm gen_config ./model --quantization q4f16_1 --conv-template qwen2 -o ./dist
          mlc_llm convert_weight ./model --quantization q4f16_1 -o ./dist
          ls -lh ./dist

      - name: Upload to HF
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          hf upload WebAIPocket/Serow-Qwen2.5-0.5B-Instruct-q4f16_1-MLC ./dist --repo-type model --token $HF_TOKEN
